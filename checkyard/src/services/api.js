
const BASE_URL = process.env.REACT_APP_BASE_URL;

export const signup = {
    signupApi : BASE_URL + "signup",
}

export const login = {
    loginApi : BASE_URL + "login",
}

export const data = {
    dataApi : BASE_URL + "machineRegister",
}

export const machineData = {
    dashboard : BASE_URL + "dashboard",
}

export const mail = {
    mailApi : BASE_URL + "mail",
}

export const graph = {
    graphApi : BASE_URL + "graph",
}